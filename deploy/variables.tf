variable default_region {
    description = "the default region for all aws sesrvices"
    type = string
}
variable app_name {
    description = "the prefix-name of the services in aws"
    type = string
}
variable "domain_name" {
  description = "the default domain name"
  type = string
}